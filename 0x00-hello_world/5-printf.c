#include <stdio.h>

/**
 * main - Prints a string using printf
 *
 * Description - Prints out the string
 * "with proper grammar, but the outcome is
 * a piece of art," using printf
 * Return: Returns zero
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
